[package]
name = "quad-gamepad"
version = "0.1.0"
edition = "2021"
description = "Gamepad support for miniquad/macroquad (W3C Gamepad API for WASM, gilrs for native)"
license = "MIT OR Apache-2.0"

[dependencies]

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
gilrs = "0.11"
